name: accountselecttile
category: LAYOUT
type: DECLARATIVE
definition:
  - uesio/io.tile:
      uesio.variant: uesio/crm.item
      content:
        - uesio/io.titlebar:
            uesio.variant: uesio/crm.item
            uesio.styleTokens:
              root:
                - items-center
            title: ${name}
            subtitle: ${description}
            actions:
              - uesio/io.text:
                  uesio.styleTokens:
                    root:
                      - bg-green-600
                      - text-white
                  uesio.display:
                    - type: hasValue
                      value: $Prop{selected}
                  uesio.variant: uesio/crm.avatariconoutline
                  text: check
      avatar:
        - uesio/io.avatar:
            text: ${initials}
            image: $UserFile{image}
      signals: $Prop{signals}
properties:
  - type: SIGNALS
    name: signals
    label: Signals
title: Account Tile Component
discoverable: true
description: An account tile
sections:
  - type: HOME
    properties:
  - type: DISPLAY
